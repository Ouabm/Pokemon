#include "move.hpp"

move::move(std::string nom, int puissance, int precision, std::string type, std::string texturepath)
    : nom(nom),puissance(puissance), precision(precision),type(type),texturepath(texturepath) {
}
