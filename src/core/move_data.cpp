#include "move_data.hpp"

// Initialisation des Moves
const Move flamethrower("Flamethrower", Type::Fire, 90, 100);
const Move fly("Fly", Type::Flying, 90, 95);
const Move dragonClaw("Dragon Claw", Type::Dragon, 80, 95);
const Move slash("Slash", Type::Normal, 70, 100);
